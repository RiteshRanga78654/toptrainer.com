/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "randomuser.me", "service.ireedindia.com", "i.pravatar.cc"],
  },
};

export default nextConfig;